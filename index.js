let input = document.querySelector('input');
let myNumder = 10;

let sendNumber = function() {
  if (input.value === myNumber) {
    alert ('you right');
  };

  if (input.value < myNumber) {
    alert ('add more');
  };

  if (input.value > myNumber) {
    alert ('add less');
  };
};
